{
  "name": "file-deduplication",
  "version": "0.1.0",
  "description": "Electron file deduplication app",
  "main": "main.js",
  "scripts": {
    "build:backend": "node build-backend.js",
    "prestart": "npm run build:backend",
    "start": "electron ."
  },
  "license": "MIT",
  "devDependencies": {
    "electron": "^37.2.0"
  }
}