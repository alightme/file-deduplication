# Repository Guidelines（仓库指南）

file-deduplication：基于 Electron 37 的桌面应用（JavaScript）。本规范适用于所有贡献者与 AI 助手，须严格执行。

## 项目结构

源代码位于仓库根目录：

- `main.js` — 主进程：应用生命周期、窗口管理。
- `preload.js` — 预加载脚本：渲染进程访问特权 API 的桥接层。
- `index.html` — 渲染进程 UI。
- `package.json` — 依赖与 npm 脚本。

## 常用命令

- `npm install` — 安装依赖。
- `npm start` — 本地启动应用。

## 代码规范

- **单一职责**：每个函数、模块只做一件事。
- **中文注释**：所有注释必须使用中文编写（命令、API、专有名词可保留原文）。
- **函数注释**：函数必须注明作用与执行流程（入参、处理步骤、返回值）。
- **字段注释**：实体类属性字段必须逐字段注明含义。
- **风格**：2 空格缩进、分号、单引号；CommonJS；文件小写连字符命名（如 `main.js`）。未配置 lint 工具，风格与现有代码保持一致。

## Agent规范

- **禁止自动提交/推送**：即使被要求，也须经用户二次确认后才可执行 `git commit` / `git push`。
- **禁止推测需求**：简洁实现明确诉求，不擅自扩展；有疑问先提问，确认后再开发。

